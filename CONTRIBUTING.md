# 参与贡献

感谢提交问题和改进。请先阅读 README 与 `docs/实现状态与外部依赖.md`，区分本地业务能力和未接入的外部服务。

## 开发流程

1. 使用 Node.js 22，运行 `npm ci`。
2. 从 `main` 新建功能分支，保持改动范围集中。
3. 为业务变化添加 `scripts/*-smoke.ts` 测试，只使用 `data/` 下独立的测试数据库。
4. 运行 `npm run build` 与 `npm run health`，前端改动还需检查实际页面。
5. 提交 Pull Request，说明问题、解决方式、验收结果和已知限制。

不要提交真实客户信息、电话、合同、数据库、账号密码、API 密钥、附件或运行日志。测试中的演示账号仅供本机使用。

数据库变更新增迁移文件，不修改已发布迁移。变更权限、审批、收付款或通知时，补充跨店隔离、重复操作和失败路径测试。

提交内容应当是你有权贡献的代码。第三方依赖保留其原许可证，不提交商业软件安装包或复制的私有实现。
