# 给 AI：从这里开始（自研中介业务系统）

> **用户意图**：把本文件夹交给 AI，按文档**全自动开发**一套自有中介业务系统。  
> **本包即为交付物料**——优先读本文件，再按顺序打开 `docs/` 内文档开工。

---

## 1. 你是谁、要做什么

你是**开发 AI**。请根据本包文档，实现一套**自有中介业务系统**（门店可用、可本地演示）。

- **功能对齐**：以总文档第二编 **M00～M22** 原版能力地图为准，按分期实现对应业务能力。
- **硬性禁止**：
  - 禁止破解厂商授权、拷贝厂商安装包、License 绕过或逆向绕过保护；
  - 禁止把厂商闭源二进制/安装介质当作「可分发产品」直接交付；
  - 允许：阅读本包需求/功能文档，从零实现自有代码与数据模型。

---

## 2. 必读顺序（严格按此）

1. **本文件** `00-给AI-从这里开始.md`（你正在读）
2. **`docs/自研中介系统-开发总文档.md`**（MASTER-2.1，权威主文档）
   - 以总文档内**预填分期勾选**为准：**MVP → P2 → P3**；标注为「暂不做 / 禁不做」的项不要做。
3. 需要字段/菜单细节时再读：
   - `docs/MVP需求规格.md`
   - `docs/功能说明书.md`
   - `docs/ERP菜单与功能清单.md`
   - `docs/软件设计方案-架构UI交互功能.md`（若有）
   - `docs/业主偏好包-模板.md`（口径问卷，若有）

权威冲突时：**以 `自研中介系统-开发总文档.md` 的预填勾选与分期为准**；细节字段参考 MVP / 功能说明书。

---

## 3. 开发优先级

1. 打开总文档 **§3.P 排期总览**，以及第三编中勾选为 **MVP必须** 的项。
2. **先跑通门店主链路（MVP）**，再做 P2，再做 P3。
3. 不要一上来铺全量 M00～M22；未勾选或「暂不做」的模块延后。

---

## 4. 技术建议（可调整，但须可本地演示）

任选其一即可，优先保证本地可跑、可演示：

- **推荐（Windows 桌面）**：Electron + 本地 SQLite（或总文档第六编架构建议）
- **亦可**：前后端分离（如 Web 前端 + 本地/局域网 API），但需一键或少量步骤即可本地演示

数据与权限模型、模块边界以总文档为准；UI 可参考设计方案文档，不必像素级复刻原版。

---

## 5. 验收标准（MVP 可演示即过关）

至少能演示：

| 能力 | 说明 |
|------|------|
| 登录 | 账号登录进入系统 |
| 房源 | 录入/查询基础房源 |
| 客源 | 录入/查询基础客源 |
| 跟进 | 对房/客写跟进记录 |
| 带看 | 登记带看 |
| 成交 | 登记基础成交 |
| 基础权限 | 不同角色可见范围或操作有区分 |

更细的字段与规则见 `MVP需求规格.md` 与总文档 MVP 相关章节。

---

## 6. 权威与冲突处理

| 问题 | 以谁为准 |
|------|----------|
| 做什么 / 先做什么 / 暂不做 | **`docs/自研中介系统-开发总文档.md`** 预填勾选与 §3.P |
| 字段、表单、菜单细项 | MVP需求规格、功能说明书、ERP菜单清单 |
| 架构与界面交互细稿 | 软件设计方案（若有）；可落地即可 |

---

## 7. 开工检查清单（给 AI）

- [ ] 已读本文件，明确禁止项与验收项
- [ ] 已读总文档第二编能力地图 + 第三编勾选 + **§3.P**
- [ ] 已列出本迭代 MVP 必须项任务清单
- [ ] 已选定可本地演示的技术栈并搭好空壳
- [ ] 按主链路实现：登录 → 房源 → 客源 → 跟进 → 带看 → 成交 → 基础权限
- [ ] 自测可演示后，再进入 P2

---

**一句话**：先读本文件 → 再读 `docs/自研中介系统-开发总文档.md`（按预填 MVP→P2→P3）→ 从门店主链路开始写自有代码，禁止破解/拷贝厂商包。
