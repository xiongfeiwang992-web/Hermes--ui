# 安全说明

这是供本地运行与二次开发的早期版本，不是经过生产安全审计的托管服务。

- API 仅监听 `127.0.0.1`。不要把演示环境转发到公网或共享默认账号。
- 浏览器来源限制为本机网页端口和默认 Vite 开发端口；外站和 `null` 来源被拒绝。桌面生产端默认通过本机 HTTP 页面加载，不从 `file://` 调用 API。
- `npm run seed` 只初始化新数据库；已有数据库会拒绝覆盖。导出的 `seedDatabase` 函数用于测试夹具，会重建显式指定的测试库，不应对业务库调用。
- 演示密码公开且固定。真实使用前需修改全部账号密码，并评估访问控制、会话、备份、磁盘权限、HTTPS、数据加密和部署隔离。
- `data/` 包含数据库、业务附件、备份和测试产物，不应上传到 GitHub。备份和本机文件访问属于管理员能力，不应视为适用于公网多租户的隔离边界。
- 本地签署记录不是第三方 CA 电子签章。外部服务配置入口不代表供应商已接通。

## 报告漏洞

请使用仓库 Security 页面中的私密漏洞报告入口（如已启用）。不要在公开 Issue 中粘贴真实凭据、客户信息或可直接利用的生产地址。若私密入口不可用，请先发不含敏感细节的 Issue 请求联系渠道。
